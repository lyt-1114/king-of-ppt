interface:
  display_name: "Image2PPT Exact"
  short_description: "Exact image deck export and editable PPTX rebuild routes."
  brand_color: "#0F766E"
  default_prompt: "Use $image2ppt-exact to convert approved slide images into exact SVG proof assets and an editable PPTX rebuild."

policy:
  allow_implicit_invocation: true
